# Use the latest version of Ubuntu
FROM ubuntu:latest

# Update APT package index and upgrade the system
RUN apt-get update && apt-get upgrade -y

# Command that will run when the container starts
CMD ["echo", "Hello, World!"]
